{
  "version": 2,
  "builds": [
    { "src": "api/**/*.js", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/$1" },
    { "src": "/trip/(.*)", "dest": "/api/trip" }
  ],
  "functions": {
    "api/trip/index.js": { "maxDuration": 10 }
  }
}